-- [[ TỰ ĐỘNG ĐỢI GAME LOAD HOÀN TOÀN TRÁNH CRASH MENU ]] --
if not game:IsLoaded() then game.Loaded:Wait() end
repeat task.wait() until game:GetService("Players").LocalPlayer and game:GetService("Players").LocalPlayer.Character

-- [[ KHỞI TẠO HỆ THỐNG PHÒNG THỦ LỖI UI (ANTI-CRASH) ]] --
local Fluent, SaveManager, InterfaceManager
local success, err = pcall(function()
    Fluent = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/main.lua"))()
    SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
    InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()
end)

-- Nếu lỗi do Executor không tải được link Github bên ngoài
if not success or not Fluent then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "⚠️ LỖI TẢI MENU ⚠️",
        Text = "Không tải được thư viện UI! Hãy BẬT VPN (1.1.1.1) và thử lại!",
        Duration = 10
    })
    return
end

-- [[ DATVNMC HUB - QUANTUM ONYX STYLE EDITION 2026 ]] --
local DatVNMC_ImageID = "rbxassetid://108529748091064"

local Window = Fluent:CreateWindow({
    Title = "DatVNMC Hub — QuantumOnyx",
    SubTitle = "by DatVNMC",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true, 
    Theme = "Dark", 
    MinimizeKey = Enum.KeyCode.LeftControl 
})

-- Cấu hình trạng thái hệ thống (Toggles)
_G.AutoFarmLevel = false
_G.AutoAttack = false
_G.BringMob = false
_G.KillAuraDragonStorm = false
_G.AntiAFK = true

-- Cấu hình Dungeon & Raid Toggles
_G.AutoBuyChip = false
_G.AutoStartRaid = false
_G.AutoKillAuraRaid = false
_G.AutoNextIslandRaid = false

-- Cấu hình PvP & ESP Toggles
_G.AimSkill = false
_G.SpeedHack = false
_G.SpeedValue = 16
_G.WalkOnWater = false
_G.PlayerESP = false
_G.FruitESP = false
_G.ChestESP = false
_G.IslandESP = false

-- Cấu hình Race V4 Toggles
_G.AutoLookMoon = false
_G.AutoPickupGear = false
_G.TweenMirage = false

-- Cấu hình Sea Events Toggles
_G.AutoSeaEvent = false
_G.AutoAttackSea = false
_G.AutoSitBoat = false
_G.FlyToRoughSea = false

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local VirtualUser = game:GetService("VirtualUser")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")

-- Khởi tạo các Tabs
local Tabs = {
    Main = Window:AddTab({ Title = "Auto Farm", Icon = "home" }),
    Dungeon = Window:AddTab({ Title = "Dungeon & Fruits", Icon = "activity" }), 
    Combat = Window:AddTab({ Title = "Combat PvP", Icon = "swords" }),
    ESP = Window:AddTab({ Title = "Hệ Thống ESP", Icon = "eye" }),
    RaceV4 = Window:AddTab({ Title = "Up Tộc V4", Icon = "moon" }),
    SeaEvent = Window:AddTab({ Title = "Sea Events", Icon = "ship" }),
    Teleport = Window:AddTab({ Title = "Teleport", Icon = "map-pin" }),
    Misc = Window:AddTab({ Title = "Misc & Shop Võ", Icon = "settings" })
}

-- ====================================================================
-- [NÚT NỔI ĐỂ MỞ/ĐÓNG MENU KHI BỊ ẨN]
-- ====================================================================
task.spawn(function()
    if CoreGui:FindFirstChild("DatVNMC_OnyxToggle") then 
        CoreGui["DatVNMC_OnyxToggle"]:Destroy() 
    end

    local ToggleScreenGui = Instance.new("ScreenGui")
    ToggleScreenGui.Name = "DatVNMC_OnyxToggle"
    ToggleScreenGui.Parent = CoreGui
    ToggleScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    local OpenCloseBtn = Instance.new("ImageButton")
    OpenCloseBtn.Name = "OpenCloseBtn"
    OpenCloseBtn.Parent = ToggleScreenGui
    OpenCloseBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    OpenCloseBtn.Position = UDim2.new(0, 15, 0, 140) 
    OpenCloseBtn.Size = UDim2.new(0, 50, 0, 50)
    OpenCloseBtn.Image = DatVNMC_ImageID
    OpenCloseBtn.ScaleType = Enum.ScaleType.Crop
    OpenCloseBtn.ZIndex = 9999

    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 25) 
    UICorner.Parent = OpenCloseBtn
    
    local UIStroke = Instance.new("UIStroke")
    UIStroke.Thickness = 2
    UIStroke.Color = Color3.fromRGB(170, 0, 255) 
    UIStroke.Parent = OpenCloseBtn

    OpenCloseBtn.Active = true
    OpenCloseBtn.Draggable = true

    OpenCloseBtn.MouseButton1Click:Connect(function()
        VirtualUser:SetKeyDown("LeftControl")
        task.wait(0.05)
        VirtualUser:SetKeyUp("LeftControl")
    end)
end)

function ToTween(pos)
    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then return end
    local distance = (pos.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
    local speed = 300
    local tweenInfo = TweenInfo.new(distance / speed, Enum.EasingStyle.Linear)
    local tween = TweenService:Create(LocalPlayer.Character.HumanoidRootPart, tweenInfo, {CFrame = pos})
    tween:Play()
    return tween
end

-- TAB 1: AUTO FARM
Tabs.Main:AddParagraph({ Title = "DatVNMC — Hệ Thống Farm Cấp", Content = "Tự động hoàn toàn, tối ưu hóa tốc độ nhận nhiệm vụ." })

local FarmToggle = Tabs.Main:AddToggle("AutoFarmLevel", {Title = "Auto Farm Level (All Seas)", Default = false})
FarmToggle:OnChanged(function()
    _G.AutoFarmLevel = Fluent.Options.AutoFarmLevel.Value
    _G.AutoAttack = Fluent.Options.AutoFarmLevel.Value
    _G.BringMob = Fluent.Options.AutoFarmLevel.Value
end)

local StormToggle = Tabs.Main:AddToggle("KillAuraDragonStorm", {Title = "Kill Aura Dragon Storm (Auto Skill Z)", Default = false})
StormToggle:OnChanged(function()
    _G.KillAuraDragonStorm = Fluent.Options.KillAuraDragonStorm.Value
end)

-- TAB 2: DUNGEON & RAIDS
local RaidSec = Tabs.Dungeon:AddSection("Quản Lý Raid Dungeon")
local BuyChipTgl = Tabs.Dungeon:AddToggle("AutoBuyChip", {Title = "Auto Mua Chip Raid (Flame)", Default = false})
BuyChipTgl:OnChanged(function() _G.AutoBuyChip = Fluent.Options.AutoBuyChip.Value end)

local StartRaidTgl = Tabs.Dungeon:AddToggle("AutoStartRaid", {Title = "Auto Nhấn Nút Khởi Hành Raid", Default = false})
StartRaidTgl:OnChanged(function() _G.AutoStartRaid = Fluent.Options.AutoStartRaid.Value end)

local KillAuraRaidTgl = Tabs.Dungeon:AddToggle("AutoKillAuraRaid", {Title = "Auto Kill Aura & Gom Quái Raid", Default = false})
KillAuraRaidTgl:OnChanged(function() _G.AutoKillAuraRaid = Fluent.Options.AutoKillAuraRaid.Value end)

local NextIslandTgl = Tabs.Dungeon:AddToggle("AutoNextIslandRaid", {Title = "Auto Dịch Chuyển Sang Đảo Tiếp Theo", Default = false})
NextIslandTgl:OnChanged(function() _G.AutoNextIslandRaid = Fluent.Options.AutoNextIslandRaid.Value end)

local FruitSec = Tabs.Dungeon:AddSection("Gacha & Lưu Trữ Trái Ác Quỷ")
Tabs.Dungeon:AddButton({
    Title = "Gacha / Random Trái Ác Quỷ (Từ Xa)",
    Callback = function() game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Cousin", "Buy") end
})
Tabs.Dungeon:AddButton({
    Title = "Cất Tất Cả Trái Ác Quỷ Vào Kho (Store Fruit)",
    Callback = function()
        for _, v in pairs(LocalPlayer.Character:GetChildren()) do
            if v:IsA("Tool") and string.find(v.Name, "Fruit") then
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("StoreFruit", v.DataFolder.FruitID.Value, v)
            end
        end
        for _, v in pairs(LocalPlayer.Backpack:GetChildren()) do
            if v:IsA("Tool") and string.find(v.Name, "Fruit") then
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("StoreFruit", v.DataFolder.FruitID.Value, v)
            end
        end
    end
})

-- TAB 3: COMBAT PVP
local AimToggle = Tabs.Combat:AddToggle("AimSkill", {Title = "Aim Skill (Khóa Mục Tiêu)", Default = false})
AimToggle:OnChanged(function() _G.AimSkill = Fluent.Options.AimSkill.Value end)

local WaterToggle = Tabs.Combat:AddToggle("WalkOnWater", {Title = "Đứng Trên Nước (Walk On Water)", Default = false})
WaterToggle:OnChanged(function(state)
    _G.WalkOnWater = state
    if state then
        local waterFloor = Instance.new("Part")
        waterFloor.Name = "DatVNMC_WaterFloor"
        waterFloor.Size = Vector3.new(50000, 2, 50000)
        waterFloor.Position = Vector3.new(0, -2, 0)
        waterFloor.Anchored = true
        waterFloor.Transparency = 1
        waterFloor.Parent = workspace
    else
        local floor = workspace:FindFirstChild("DatVNMC_WaterFloor")
        if floor then floor:Destroy() end
    end
end)

local SpeedSlider = Tabs.Combat:AddSlider("SpeedSlider", {
    Title = "Tốc Độ Di Chuyển (Speed)",
    Default = 16, Min = 16, Max = 100, Rounding = 0,
    Callback = function(Value) _G.SpeedValue = Value _G.SpeedHack = true end
})

-- TAB 4: ESP
local P_ESP = Tabs.ESP:AddToggle("PlayerESP", {Title = "Định Vị Người Chơi", Default = false})
P_ESP:OnChanged(function() _G.PlayerESP = Fluent.Options.PlayerESP.Value end)
local F_ESP = Tabs.ESP:AddToggle("FruitESP", {Title = "Định Vị Trái Ác Quỷ", Default = false})
F_ESP:OnChanged(function() _G.FruitESP = Fluent.Options.FruitESP.Value end)
local C_ESP = Tabs.ESP:AddToggle("ChestESP", {Title = "Định Vị Rương", Default = false})
C_ESP:OnChanged(function() _G.ChestESP = Fluent.Options.ChestESP.Value end)
local I_ESP = Tabs.ESP:AddToggle("IslandESP", {Title = "Định Vị Đảo", Default = false})
I_ESP:OnChanged(function() _G.IslandESP = Fluent.Options.IslandESP.Value end)

-- TAB 5: UP TỘC V4
local MirageTgl = Tabs.RaceV4:AddToggle("TweenMirage", {Title = "Auto Tìm Đến Đảo Mirage", Default = false})
MirageTgl:OnChanged(function() _G.TweenMirage = Fluent.Options.TweenMirage.Value end)
local MoonTgl = Tabs.RaceV4:AddToggle("AutoLookMoon", {Title = "Auto Nhìn Trăng", Default = false})
MoonTgl:OnChanged(function() _G.AutoLookMoon = Fluent.Options.AutoLookMoon.Value end)
local GearTgl = Tabs.RaceV4:AddToggle("AutoPickupGear", {Title = "Auto Nhặt Bánh Răng", Default = false})
GearTgl:OnChanged(function() _G.AutoPickupGear = Fluent.Options.AutoPickupGear.Value end)

-- TAB 6: SEA EVENTS
local SeaEventTgl = Tabs.SeaEvent:AddToggle("AutoSeaEvent", {Title = "Auto Diệt Sea Events", Default = false})
SeaEventTgl:OnChanged(function() _G.AutoSeaEvent = Fluent.Options.AutoSeaEvent.Value end)
local SeaAttackTgl = Tabs.SeaEvent:AddToggle("AutoAttackSea", {Title = "Auto Click Tấn Công Quái Biển", Default = false})
SeaAttackTgl:OnChanged(function() _G.AutoAttackSea = Fluent.Options.AutoAttackSea.Value end)
local SitBoatTgl = Tabs.SeaEvent:AddToggle("AutoSitBoat", {Title = "Auto Ngồi Ghế Lái Thuyền", Default = false})
SitBoatTgl:OnChanged(function() _G.AutoSitBoat = Fluent.Options.AutoSitBoat.Value end)
local FlySeaTgl = Tabs.SeaEvent:AddToggle("FlyToRoughSea", {Title = "Auto Bay Ra Biển Động Zone 6", Default = false})
FlySeaTgl:OnChanged(function() _G.FlyToRoughSea = Fluent.Options.FlyToRoughSea.Value end)

-- TAB 7: TELEPORT
Tabs.Teleport:AddButton({ Title = "Đi Sea 1", Callback = function() game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("TravelMain") end })
Tabs.Teleport:AddButton({ Title = "Đi Sea 2", Callback = function() game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("TravelDressrosa") end })
Tabs.Teleport:AddButton({ Title = "Đi Sea 3", Callback = function() game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("TravelZou") end })

-- TAB 8: MISC & VÕ
local FightingStyles = {
    ["Godhuman (Ancient Monk)"] = Vector3.new(-2870, 246, -14340),
    ["Superhuman (Martial Arts Master)"] = Vector3.new(-11466, 332, -441),
    ["Death Step (Phoenis)"] = Vector3.new(-5489, 90, -1405),
    ["Sharkman Karate (Daigrock the Shark)"] = Vector3.new(-2828, 7, -10045),
    ["Electric Claw (Previous Hero)"] = Vector3.new(-9510, 316, -5720),
    ["Dragon Talon (Uzoth)"] = Vector3.new(8955, 30, -13795)
}
local StyleDropdown = Tabs.Misc:AddDropdown("FightingStyleDropdown", {
    Title = "Chọn Kiểu Võ Muốn Học",
    Values = {"Godhuman (Ancient Monk)", "Superhuman (Martial Arts Master)", "Death Step (Phoenis)", "Sharkman Karate (Daigrock the Shark)", "Electric Claw (Previous Hero)", "Dragon Talon (Uzoth)"},
    CurrentValue = "Godhuman (Ancient Monk)",
    Callback = function(Option) _G.SelectedFightingStyle = Option end
})
Tabs.Misc:AddButton({
    Title = "Bay Đến NPC Học Võ Đã Chọn",
    Callback = function()
        if _G.SelectedFightingStyle and FightingStyles[_G.SelectedFightingStyle] then
            ToTween(CFrame.new(FightingStyles[_G.SelectedFightingStyle]))
        end
    end
})

Tabs.Misc:AddButton({
    Title = "Xóa Đồ Họa (Giảm Lag FPS)",
    Callback = function()
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") and not v:IsA("MeshPart") then v.Material = Enum.Material.SmoothPlastic v.Reflectance = 0
            elseif v:IsA("Decal") or v:IsA("Texture") then v:Destroy() end
        end
        Lighting.GlobalShadows = false
    end
})

-- ====================================================================
-- [HỆ THỐNG ENGINE CHẠY NGẦM]
-- ====================================================================
task.spawn(function()
    while true do
        task.wait(0.1)
        if _G.KillAuraDragonStorm then
            pcall(function()
                VirtualUser:SetKeyDown("z")
                task.wait(0.1)
                VirtualUser:SetUpKey("z")
            end)
        end
    end
end)

if _G.AntiAFK then
    LocalPlayer.Idled:Connect(function()
        VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    end)
end

-- LOGIC AUTO SEA EVENT ENGINE
task.spawn(function()
    while true do
        task.wait(0.2)
        if _G.AutoSeaEvent then
            pcall(function()
                local TargetSeaMonster = nil
                for _, v in pairs(workspace:GetChildren()) do
                    if (v.Name == "Sea Beast" or v.Name == "Terrorshark" or string.find(v.Name, "Ship") or string.find(v.Name, "Shark")) and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 and v:FindFirstChild("HumanoidRootPart") then
                        TargetSeaMonster = v
                        break
                    end
                end
                if TargetSeaMonster then
                    ToTween(TargetSeaMonster.HumanoidRootPart.CFrame * CFrame.new(0, 35, 0))
                    if _G.AutoAttackSea then
                        VirtualUser:CaptureController()
                        VirtualUser:ClickButton1(Vector2.new(50, 50))
                    end
                else
                    if _G.FlyToRoughSea then
                        ToTween(CFrame.new(LocalPlayer.Character.HumanoidRootPart.Position.X + 200, 45, LocalPlayer.Character.HumanoidRootPart.Position.Z + 200))
                    end
                end
            end)
        end
    end
end)

-- LOGIC AUTO NGỒI GHẾ LÁI
task.spawn(function()
    while true do
        task.wait(0.5)
        if _G.AutoSitBoat then
            pcall(function()
                if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") and LocalPlayer.Character.Humanoid.SeatPart == nil then
                    for _, v in pairs(workspace:GetDescendants()) do
                        if v:IsA("VehicleSeat") and (v.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude < 25 then
                            v:Sit(LocalPlayer.Character.Humanoid)
                            break
                        end
                    end
                end
            end)
        end
    end
end)

-- LOGIC FARM LEVEL
local CurrentQuestName = ""
local CurrentMobName = ""
function CheckQuestAndMob()
    local myLevel = LocalPlayer.Data.Level.Value
    if myLevel >= 1 and myLevel < 15 then CurrentQuestName = "BanditQuest1" CurrentMobName = "Bandit"
    elseif myLevel >= 15 and myLevel < 30 then CurrentQuestName = "MonkeyQuest1" CurrentMobName = "Monkey"
    else CurrentQuestName = "BanditQuest1" CurrentMobName = "Bandit" end
end

task.spawn(function()
    while true do
        task.wait(0.1)
        if _G.AutoFarmLevel then
            pcall(function()
                CheckQuestAndMob()
                local hasQuest = LocalPlayer.PlayerGui.Main:FindFirstChild("Quest") and LocalPlayer.PlayerGui.Main.Quest.Visible
                if not hasQuest then
                    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("StartQuest", CurrentQuestName, 1)
                else
                    local targetMob = nil
                    for _, mob in pairs(workspace.Enemies:GetChildren()) do
                        if mob.Name == CurrentMobName and mob:FindFirstChild("Humanoid") and mob.Humanoid.Health > 0 then
                            targetMob = mob
                            break
                        end
                    end
                    if targetMob and targetMob:FindFirstChild("HumanoidRootPart") then
                        ToTween(targetMob.HumanoidRootPart.CFrame * CFrame.new(0, 10, 0))
                        if _G.AutoAttack then
                            VirtualUser:CaptureController()
                            VirtualUser:ClickButton1(Vector2.new(50, 50))
                        end
                        if _G.BringMob then
                            for _, otherMob in pairs(workspace.Enemies:GetChildren()) do
                                if otherMob.Name == CurrentMobName and otherMob ~= targetMob and otherMob:FindFirstChild("HumanoidRootPart") then
                                    otherMob.HumanoidRootPart.CFrame = targetMob.HumanoidRootPart.CFrame
                                    otherMob.HumanoidRootPart.CanCollide = false
                                end
                            end
                        end
                    else
                        for _, spawnPoint in pairs(workspace._WorldOrigin.EnemySpawns:GetChildren()) do
                            if string.find(spawnPoint.Name, CurrentMobName) then
                                ToTween(spawnPoint.CFrame * CFrame.new(0, 20, 0))
                                break
                            end
                        end
                    end
                end
            end)
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if _G.SpeedHack and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = _G.SpeedValue
    end
end)

Window:SelectTab(1)
